name: Authenticated DAST integration accountability

on:
  workflow_dispatch:
  schedule:
    - cron: "23 6 * * 2"

permissions:
  contents: read

jobs:
  controlled-authenticated-dast:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Check out the trusted controlled fixture without credentials
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2, Node 24, verified 2026-07-22
        with:
          persist-credentials: false
      - name: Install pinned validation dependency
        run: python3 -m pip install --disable-pip-version-check --requirement requirements.txt
      - name: Exercise authenticated passive ZAP against the controlled internal fixture
        env:
          VIBESEC_AUTH_BEARER_TOKEN: ${{ secrets.VIBESEC_AUTH_FIXTURE_BEARER }}
        run: python3 scripts/test_dast_container.py --authenticated
