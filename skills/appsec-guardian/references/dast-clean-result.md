# Clean passive DAST result

Say only that the passive baseline completed within its configured unauthenticated crawl scope without a reportable result. Do not claim the service is secure or that authorization, business logic, authenticated surfaces, injection, or unlinked endpoints were tested.
