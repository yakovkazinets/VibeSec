# Mutable DAST image

A tag such as `app:latest` is ineligible. Do not resolve it silently, build a Dockerfile, or substitute a different image. Request a reviewed immutable digest reference and report the add-on as not configured until it is supplied.
