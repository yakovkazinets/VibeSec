# DAST on a pull request

Do not start the target or scanner. Record `not_configured` because the event is untrusted, keep secrets absent, and never introduce `pull_request_target` as a workaround. Review the change first and run manually or on schedule from trusted code.
