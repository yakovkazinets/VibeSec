# Root DAST image

An image with empty, `root`, UID `0`, or `0:<group>` user metadata fails closed. Do not override the image user to make the scan run. Ask the image owner to publish and validate a non-root image revision.
