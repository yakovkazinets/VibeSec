# Monorepository

Inventory every manifest root, source ecosystem, nested Dockerfile, IaC path, and workflow. Retain repository-relative paths and confirm one ecosystem does not suppress another. Recommend Standard only when the broader routing/SBOM/IaC depth is wanted and owned; otherwise a fast Minimal baseline remains valid. Report traversal and skipped-directory limits.
