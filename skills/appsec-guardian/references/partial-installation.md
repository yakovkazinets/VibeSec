# Partial VibeSec installation

Evidence: a VibeSec workflow, support files, or installation manifest exists without the matching set/version. Classify this as an adoption error. Run preflight and initializer dry run, preserve baselines/suppressions, report conflicts, and compare version-compatible files. Never overwrite or infer that a partial workflow is safe to run.
