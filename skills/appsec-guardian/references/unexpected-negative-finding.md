# Unexpected negative-fixture finding

Report the capability ID, new safe finding metadata, and negative fixture path without source snippets. Determine whether scanner behavior improved, the fixture became ambiguous, or a false positive appeared. Require review before changing the fixture, count, severity, or suppression; never broadly ignore the rule merely to make CI pass.
