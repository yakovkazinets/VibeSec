# Consumer fixtures

These repositories are inert structure fixtures. They are never built, installed, deployed, or sent over the network. Each `expected.json` records the detector evidence and intended test purpose. Package names are fictional and no credential-like strings are included.

The `symlink-boundary` escape link is created dynamically by the test suite because archive and patch tooling do not portably preserve an intentionally broken external symlink.
