# Checkov IaC fixture

The positive Terraform is inert text and is never initialized, planned, or applied. Controlled Checkov JSON validates normalization; integration tests enforce the immutable, network-disabled, read-only container invocation.
