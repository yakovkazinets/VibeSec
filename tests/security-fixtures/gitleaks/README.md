# Gitleaks fixture

The controlled marker `VIBESEC_FAKE_SECRET_DO_NOT_USE_000000000000` is not a usable credential and cannot authenticate to a live service. The negative variant is similar prose without the controlled marker.
