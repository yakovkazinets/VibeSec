# Opengrep fixture

Each current VibeSec Guardian-owned language rule has exactly one non-operational positive and a nearby safe negative. Controlled JSON validates normalized metadata; the same source files are also executed by the pinned Opengrep rule test.
