# OSV-Scanner fixture

The dependency name and advisory are synthetic. Controlled OSV v2-shaped output prevents CI expectations from depending on a mutable live database. Self-scan separately asserts the configured online or offline state.
