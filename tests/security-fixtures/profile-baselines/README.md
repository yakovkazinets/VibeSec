# Profile baseline fixture

The positive scenario keeps Minimal and Standard fingerprints separate. The negative scenario swaps them and must fail configuration validation rather than suppress findings.
