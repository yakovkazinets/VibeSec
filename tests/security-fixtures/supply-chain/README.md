# Supply-chain assurance fixtures

The scenario catalog drives offline accountability tests for deterministic
checksums, strict manifests, provenance subjects, tampering, malformed and
oversized metadata, signature boundaries, triggers, and permissions. No
fixture is signed and no live OIDC operation runs in required CI.
