# Trivy filesystem fixture

The positive Dockerfile is intentionally non-deployable and models a synthetic configuration finding. The negative file uses a non-root user. Controlled Trivy JSON provides deterministic normalization while repository self-scan exercises the pinned scanner.
