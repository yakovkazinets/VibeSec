# Trusted harness fixture

The positive marker represents the base-revision harness. The negative target shadows scripts, rules, configuration, policy, baselines, expectations, and result schemas. Integration tests require all scanner arguments to reference the trusted VibeSec root.
